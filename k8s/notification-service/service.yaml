apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: notifications
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 8003
  type: ClusterIP
